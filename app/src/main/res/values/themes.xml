<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Cold start on Android 12+; only used when first-launch intro runs (see Splash.java setTheme). -->
    <style name="Theme.MindMatch.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.MindMatch</item>
    </style>

    <!--
      Light-only: layouts use fixed light backgrounds (#FFF / #FAFAFA). DayNight made text/hints follow
      system dark mode → white-on-white on many screens. OEM force-dark can also break EditText contrast.
    -->
    <style name="Theme.MindMatch" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/cw_on_surface</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/cw_on_surface</item>
        <item name="android:textColorSecondary">@color/cw_on_surface_variant</item>
        <item name="android:textColorHint">@color/cw_input_hint_static</item>
        <item name="android:editTextColor">@color/cw_on_surface</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>