fastapi>=0.109.0
uvicorn[standard]>=0.27.0
supabase>=2.3.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
numpy>=1.24.0
# CPU-only PyTorch — avoids multi‑GB NVIDIA CUDA wheels on Linux (Render, Docker, etc.).
# For local GPU dev on your machine, you can temporarily use `pip install torch` without +cpu.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
